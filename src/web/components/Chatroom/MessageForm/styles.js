import styled from 'styled-components';

const Form = styled.form`
    display: flex;
    align-items: center;
`;

const InputWrapper = styled.div`
    flex-grow: 1;
`;

export { Form, InputWrapper };
